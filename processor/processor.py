from typing import List, Tuple, Calllale, Iterable


class ProcessorBase:
    """
    The base format for multiple processor, it at least include two kinds of functions -- producer and consumer, however you can define more complicated models.
    """

    def __init__(self, *args, **kwargs) -> None:
        pass
    
    def 
